from random import randint

print("Hello there! Lets generate you a roblox password! You can select one :)")


password = ""
for obj in range(8):
    obj = chr(randint(65, 95)).lower()
    password = str(password) + obj
print(password)

password = ""
for obj in range(8):
    obj = chr(randint(65, 95)).lower()
    password = str(password) + obj
print(password)

password = ""
for obj in range(8):
    obj = chr(randint(65, 95)).lower()
    password = str(password) + obj
print(password)

password = ""
for obj in range(8):
    obj = chr(randint(65, 95)).lower()
    password = str(password) + obj
print(password)

password = ""
for obj in range(8):
    obj = chr(randint(65, 95)).lower()
    password = str(password) + obj
print(password)

password = ""
for obj in range(8):
    obj = chr(randint(65, 95)).lower()
    password = str(password) + obj
print(password)

password = ""
for obj in range(8):
    obj = chr(randint(65, 95)).lower()
    password = str(password) + obj
print(password)

password = ""
for obj in range(8):
    obj = chr(randint(65, 95)).lower()
    password = str(password) + obj
print(password)

password = ""
for obj in range(8):
    obj = chr(randint(65, 95)).lower()
    password = str(password) + obj
print(password)


# Make sure to run this on a python ide :)
